# Placeholder only: replace every value and encrypt this file with sops before
# applying the NixOS configuration.
ssh:
  private: |
    -----BEGIN OPENSSH PRIVATE KEY-----
    CHANGE_ME
    -----END OPENSSH PRIVATE KEY-----
  hosts: |
    # Known-host entries for the hosts used by WIKI_REMOTE_STAGING and
    # WIKI_REMOTE_PRODUCTION, such as git.o526.net. Obtain each host key from
    # a trusted source before adding it here.
    git.o526.net CHANGE_ME
